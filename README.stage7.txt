

a) No code was reused for this stage

b) This stage is complete and everything works.

c) The TCP connection would be closed immediately (reset).

d) The RST packet is specific to the TCP protocol. RST is one of the TCP flags that can be set in the TCP header indicating the connection should be reset. ICMP and UDP are not TCP and therefore do not have RST flags.
